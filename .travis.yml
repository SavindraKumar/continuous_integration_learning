language : c
compiler : gcc
before_install:
  - sudo apt-get update -qq
install: 
  # cppcheck
  - sudo apt-get install -qq cppcheck
  
#  -stage: static_analysis
#  script:
#   - cppcheck --quiet --error-exitcode=1 --std=C99 .
    
# -stage: compile
# script: 
#  - gcc test.c -o test
#  - ./test
