language : c
compiler : gcc
before_install:
  - sudo apt-get update -qq
install: 
  # cppcheck
  - sudo apt-get install -qq cppcheck
script: 
   - gcc test.c -o test
   - ./test
   - cppcheck --quiet --error-exitcode=1 --std=C99 .
